import Button from "./components/Button/Button"

export default function Home() {
  return (
    <div>
      <Button variant="contained" color="deepPurple">
        Dodaj pozycję menu
      </Button>
      <Button variant="outlined" color="purple">
        Dodaj
      </Button>
      <Button variant="outlined" color="darkGray">
        Dodaj
      </Button>
    </div>
  )
}
